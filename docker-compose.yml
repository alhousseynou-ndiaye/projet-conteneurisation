services:
  backend:
    build: ./backend
    environment:
      - DB_PATH=/data/app.db
    volumes:
      - sqlite_data:/data
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"

volumes:
  sqlite_data:
